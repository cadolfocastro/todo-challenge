export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000',
  // Firebase Web API Key – get it from:
  // Firebase Console → Project Settings → General → Your apps → Web API Key
  firebaseApiKey: 'CLAVE_ELIMINADA_DEL_HISTORIAL',
};
